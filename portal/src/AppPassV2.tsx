import {FormEvent,useEffect,useMemo,useState} from 'react'
import {Activity,AlertTriangle,ArrowRight,Bot,Building2,CheckCircle2,ChevronRight,ClipboardCheck,Construction,FileText,Fingerprint,Gauge,HardHat,Landmark,LockKeyhole,LogOut,Milestone,Plus,Search,Send,ShieldCheck,Sparkles,TimerReset,WalletCards,Wrench,XCircle} from 'lucide-react'
import {QRCodeSVG} from 'qrcode.react'

type User={id:number;name:string;email:string;role:'admin'|'team'|'client'}
type Project={id:number;name:string;city?:string;status:string;budget:number|string;spent:number|string;progress:number|string;planned_progress:number|string;updated_at?:string}
type Req={id:number;title:string;category:string;required:boolean;status:string;evidence_count:number}
type Payment={id:number;gate_id?:number;description:string;claimed_amount:number|string;validated_amount:number|string;recommendation:string;reason?:string;status:string;created_at:string}
type Gate={id:number;phase_name:string;order_index:number;status:string;technical_score:number;released_at?:string;requirements:Req[];evidence_count:number;payment?:Payment|null}
type PassportItem={id:number;system_type:string;zone:string;title:string;description?:string;photo_url?:string;height_cm?:number;depth_cm?:number;warranty_until?:string;supplier?:string}
type Risk={code:string;title:string;severity:'baixa'|'media'|'alta'|'critica';why:string;action:string;source:string}
type Bundle={project:Project;gates:Gate[];payments:Payment[];passport:PassportItem[];riskRadar:{phase:string;score:number;level:string;items:Risk[]};summary:{released:number;blocked:number;pending:number;passportItems:number;paymentPending:number;nextGate?:Gate|null}}
type View='pass'|'payments'|'passport'|'risk'|'ai'|'manage'

async function api<T=any>(url:string,options:RequestInit={}):Promise<T>{const r=await fetch(url,{...options,credentials:'include',headers:{'Content-Type':'application/json',...(options.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Erro');return d}
const money=(v:any)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:0}).format(Number(v)||0)
const pct=(v:any)=>`${Number(v||0).toFixed(1).replace('.0','')}%`
const date=(v?:string)=>v?new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'short',year:'numeric'}).format(new Date(v)):'—'
const human=(v:string)=>String(v||'').replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase())

export default function AppPassV2(){const[user,setUser]=useState<User|null>(null);const[loading,setLoading]=useState(true);useEffect(()=>{api('/api/me').then(r=>setUser(r.user)).catch(()=>{}).finally(()=>setLoading(false))},[]);if(loading)return <Splash/>;if(!user)return <Login onLogin={setUser}/>;return <Portal user={user} onLogout={()=>setUser(null)}/>}
function Splash(){return <div className="pass-splash"><Logo/></div>}
function Logo({small=false}:{small?:boolean}){return <div className={`pass-logo ${small?'small':''}`}><span>O</span><div><b>OBRA360</b><small>PASS</small></div></div>}
function Login({onLogin}:{onLogin:(u:User)=>void}){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await api('/api/auth/login',{method:'POST',body:JSON.stringify({email,password})});onLogin(r.user)}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <div className="pass-login"><section className="pass-login-copy"><Logo/><div><span className="kicker light">PASSAPORTE VERIFICÁVEL DA OBRA</span><h1>Não pague no escuro.<br/>Não avance no improviso.</h1><p>Cada etapa reúne evidências, pendências, validação técnica, segurança e liberação financeira em um único registro.</p><div className="login-features"><span><Milestone size={17}/> Gate por etapa</span><span><WalletCards size={17}/> Medição validada</span><span><Fingerprint size={17}/> DNA do imóvel</span></div></div></section><section className="pass-login-form"><form onSubmit={submit}><span className="kicker">ACESSO AO PORTAL</span><h2>Entre na sua obra</h2><p>Use o acesso individual enviado pela equipe Obra360.</p><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="form-error">{error}</div>}<button disabled={busy}>{busy?'Entrando…':'Entrar no Obra360 PASS'} <ArrowRight size={17}/></button></form></section></div>}

function Portal({user,onLogout}:{user:User;onLogout:()=>void}){const staff=['admin','team'].includes(user.role);const[projects,setProjects]=useState<Project[]>([]);const[projectId,setProjectId]=useState<number|null>(null);const[b,setB]=useState<Bundle|null>(null);const[view,setView]=useState<View>('pass');const[menu,setMenu]=useState(false);async function loadProjects(){const r=await api('/api/projects');setProjects(r.projects);if(!projectId&&r.projects[0])setProjectId(r.projects[0].id)}async function load(){if(projectId)setB(await api(`/api/pass/projects/${projectId}`))}useEffect(()=>{loadProjects()},[]);useEffect(()=>{load()},[projectId]);async function logout(){await api('/api/auth/logout',{method:'POST'}).catch(()=>{});onLogout()}if(!projects.length)return <NoProject user={user} staff={staff} reload={loadProjects} logout={logout}/>;const items:[View,string,any][]=[['pass','Obra360 PASS',ShieldCheck],['payments','Medições & pagamentos',WalletCards],['passport','Passaporte do imóvel',Fingerprint],['risk','Radar da próxima etapa',HardHat],['ai','Obra IA',Bot],...(staff?[['manage','Gestão técnica',Wrench] as [View,string,any]]:[])];return <div className="pass-shell"><aside className={`pass-side ${menu?'open':''}`}><div className="pass-side-head"><Logo small/><button className="mobile-close" onClick={()=>setMenu(false)}><XCircle size={20}/></button></div><div className="active-project"><small>OBRA ATIVA</small><b>{b?.project.name||'Carregando…'}</b><span>{b?.project.city||'Município não informado'}</span></div><nav>{items.map(([k,l,I])=><button key={k} className={view===k?'active':''} onClick={()=>{setView(k);setMenu(false)}}><I size={18}/><span>{l}</span>{k==='payments'&&b?.summary.paymentPending?<em>{b.summary.paymentPending}</em>:null}</button>)}</nav><div className="pass-user"><div><span>{user.name[0]?.toUpperCase()}</span><p><b>{user.name}</b><small>{human(user.role)}</small></p></div><button onClick={logout}><LogOut size={16}/></button></div></aside><main className="pass-main"><header className="pass-top"><button className="pass-menu-btn" onClick={()=>setMenu(true)}><Construction size={18}/> Menu</button><div className="pass-project-select"><Building2 size={16}/><select value={projectId||''} onChange={e=>setProjectId(Number(e.target.value))}>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></div><span className="pass-top-note">última atualização {b?.project.updated_at?date(b.project.updated_at):'—'}</span></header>{!b?<div className="pass-loading">Carregando obra…</div>:<Router view={view} b={b} reload={load}/>}</main></div>}

function Router({view,b,reload}:{view:View;b:Bundle;reload:()=>void}){if(view==='pass')return <PassView b={b}/>;if(view==='payments')return <Payments b={b} reload={reload}/>;if(view==='passport')return <Passport b={b}/>;if(view==='risk')return <RiskView b={b}/>;if(view==='ai')return <AI b={b}/>;return <Manage b={b} reload={reload}/>}
function Head({tag,title,text,right}:{tag:string;title:string;text:string;right?:any}){return <div className="pass-page-head"><div><span className="kicker">{tag}</span><h1>{title}</h1><p>{text}</p></div>{right}</div>}

function PassView({b}:{b:Bundle}){const next=b.summary.nextGate;return <div className="pass-page"><Head tag="OBRA360 PASS" title="Só avance quando estiver certo." text="Cada etapa reúne o que precisa estar concluído antes do próximo serviço — e antes do próximo pagamento." right={<div className="trust-badge"><ShieldCheck size={21}/><div><small>PRÓXIMO GATE</small><b>{next?gateLabel(next.status):'Obra liberada'}</b></div></div>}/><section className="pass-hero-grid"><article className="pass-hero-card"><div className="pass-hero-top"><div><small>AVANÇO FÍSICO</small><strong>{pct(b.project.progress)}</strong></div><div><small>GATES LIBERADOS</small><strong>{b.summary.released}/{b.gates.length}</strong></div><div><small>MEDIÇÕES PENDENTES</small><strong>{b.summary.paymentPending}</strong></div></div><div className="pass-track"><i style={{width:`${Math.min(100,Number(b.project.progress||0))}%`}}/></div><p>O percentual físico é informativo. A etapa só recebe status de liberada depois do registro de validação técnica.</p></article><article className="pass-next"><span className="kicker">PRÓXIMA ETAPA</span><h3>{next?.phase_name||'Todas liberadas'}</h3>{next&&<><p>{next.requirements.filter(r=>r.required&&r.status!=='concluido').length} requisito(s) obrigatório(s) ainda pendente(s).</p><div className="next-stats"><span><FileText size={16}/>{next.requirements.length} requisitos</span><span><ClipboardCheck size={16}/>{next.evidence_count} evidências</span></div></>}</article></section><section className="gate-roadmap">{b.gates.map((g,i)=><GateCard key={g.id} gate={g} index={i}/>)}</section><section className="pass-principle"><div><LockKeyhole size={24}/><h3>Validação, não automação cega.</h3></div><p>O Obra360 organiza as evidências e sinaliza pendências. A liberação técnica permanece com o profissional competente para o serviço.</p></section></div>}
function GateCard({gate,index}:{gate:Gate;index:number}){const[open,setOpen]=useState(false);const done=gate.requirements.filter(r=>r.status==='concluido').length;const pend=gate.requirements.filter(r=>r.required&&r.status!=='concluido').length;return <article className={`gate-card gate-${gate.status}`}><button className="gate-main" onClick={()=>setOpen(!open)}><span className="gate-index">{String(index+1).padStart(2,'0')}</span><div className="gate-name"><small>{gate.status==='released'?'ETAPA LIBERADA':gate.status==='blocked'?'ETAPA BLOQUEADA':'GATE EM VALIDAÇÃO'}</small><h3>{gate.phase_name}</h3></div><div className="gate-score"><span>{done}/{gate.requirements.length}</span><small>requisitos</small></div><div className="gate-status-dot"><span/><b>{gateLabel(gate.status)}</b></div><ChevronRight size={18}/></button>{open&&<div className="gate-detail"><div className="gate-meta"><span><ClipboardCheck size={15}/>{gate.evidence_count} evidência(s)</span><span><AlertTriangle size={15}/>{pend} obrigatório(s) pendente(s)</span><span><Gauge size={15}/>{gate.technical_score||0}/100</span>{gate.released_at&&<span><CheckCircle2 size={15}/>liberada em {date(gate.released_at)}</span>}</div><div className="req-list">{gate.requirements.map(r=><div key={r.id} className={`req-row ${r.status}`}><span>{r.status==='concluido'?<CheckCircle2 size={17}/>:<AlertTriangle size={17}/>}</span><div><b>{r.title}</b><small>{human(r.category)} {r.required?'• obrigatório':'• apoio'}</small></div><em>{r.evidence_count} evid.</em></div>)}</div>{gate.payment&&<div className="gate-payment"><WalletCards size={18}/><div><small>LIBERAÇÃO FINANCEIRA</small><b>{human(gate.payment.recommendation)} • {money(gate.payment.validated_amount)}</b><p>{gate.payment.reason||'Sem justificativa registrada.'}</p></div></div>}</div>}</article>}

function Payments({b,reload}:{b:Bundle;reload:()=>void}){async function decide(id:number,status:string){if(!confirm(status==='approved'?'Confirmar liberação deste pagamento?':'Confirmar retenção deste pagamento?'))return;await api(`/api/pass/projects/${b.project.id}/payments/${id}`,{method:'PATCH',body:JSON.stringify({status})});reload()}return <div className="pass-page"><Head tag="MEDIÇÃO + EVIDÊNCIA" title="Pagar pelo que foi validado." text="A medição fica conectada ao gate, às evidências e às pendências da etapa."/><div className="payment-summary"><Metric icon={Landmark} label="Orçamento" value={money(b.project.budget)} text="Referência cadastrada"/><Metric icon={WalletCards} label="Realizado" value={money(b.project.spent)} text={`${Number(b.project.budget)?Math.round(Number(b.project.spent)/Number(b.project.budget)*100):0}% do orçamento`}/><Metric icon={ClipboardCheck} label="Em análise" value={String(b.summary.paymentPending)} text="liberação(ões) pendente(s)"/></div><div className="payment-list">{b.payments.map(p=><article className={`payment-card ${p.recommendation}`} key={p.id}><div className="payment-card-top"><div><small>{human(p.recommendation)}</small><h3>{p.description}</h3></div><Status value={p.status}/></div><div className="payment-values"><span><small>Solicitado</small><b>{money(p.claimed_amount)}</b></span><ArrowRight size={18}/><span><small>Validado</small><b>{money(p.validated_amount)}</b></span></div><p>{p.reason||'Sem justificativa registrada.'}</p><div className="payment-foot"><span>Criado em {date(p.created_at)}</span>{p.status==='pending'&&<div><button className="secondary" onClick={()=>decide(p.id,'rejected')}>Não liberar</button><button className="approve" onClick={()=>decide(p.id,'approved')}><CheckCircle2 size={15}/> Liberar</button></div>}</div></article>)}{!b.payments.length&&<EmptyState icon={WalletCards} title="Nenhuma medição cadastrada" text="Quando a equipe lançar uma medição, a recomendação aparecerá aqui."/>}</div></div>}

function Passport({b}:{b:Bundle}){const[q,setQ]=useState('');const rows=useMemo(()=>b.passport.filter(i=>(i.title+' '+i.zone+' '+i.system_type+' '+(i.description||'')).toLowerCase().includes(q.toLowerCase())),[q,b.passport]);const publicUrl=`${window.location.origin}/public/pass/${b.project.id}`;return <div className="pass-page"><Head tag="DNA DO IMÓVEL" title="O que ficará escondido também fica documentado." text="Tubulações, elétrica, impermeabilização, equipamentos, garantias e registros importantes permanecem consultáveis depois da entrega." right={<div className="passport-qr"><QRCodeSVG value={publicUrl} size={62} fgColor="#143c31"/><div><small>PASSAPORTE</small><b>registro verificável</b></div></div>}/><div className="passport-search"><Search size={16}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar: banheiro, hidráulica, parede norte…"/></div><div className="passport-grid">{rows.map(i=><article className="passport-card" key={i.id}><div className="passport-card-icon"><Fingerprint size={20}/></div><div><small>{human(i.system_type)} • {i.zone||'zona não informada'}</small><h3>{i.title}</h3><p>{i.description||'Sem descrição adicional.'}</p><div className="passport-tags">{i.height_cm!=null&&<span>altura {i.height_cm} cm</span>}{i.depth_cm!=null&&<span>profundidade {i.depth_cm} cm</span>}{i.warranty_until&&<span>garantia até {date(i.warranty_until)}</span>}{i.supplier&&<span>{i.supplier}</span>}</div>{i.photo_url&&<a href={i.photo_url} target="_blank">Ver evidência <ArrowRight size={13}/></a>}</div></article>)}{!rows.length&&<EmptyState icon={Fingerprint} title="Nenhum item encontrado" text="O passaporte cresce durante a obra, principalmente antes de fechar paredes, pisos e shafts."/>}</div></div>}

function RiskView({b}:{b:Bundle}){const r=b.riskRadar;return <div className="pass-page"><Head tag="RADAR DA PRÓXIMA ETAPA" title={`Antes de iniciar: ${r.phase||'próxima etapa'}`} text="O radar antecipa verificações técnicas, documentais e de segurança a partir da etapa seguinte e do histórico cadastrado." right={<div className={`risk-level risk-${r.level}`}><small>ÍNDICE DE ATENÇÃO</small><b>{r.score}/100</b><span>{human(r.level)}</span></div>}/><div className="risk-radar-grid">{r.items.map((i,n)=><article className={`radar-card sev-${i.severity}`} key={n}><div className="radar-top"><span>{i.severity==='critica'?<XCircle size={18}/>:i.severity==='alta'?<AlertTriangle size={18}/>:i.severity==='media'?<TimerReset size={18}/>:<ShieldCheck size={18}/>}</span><div><small>{i.source}</small><h3>{i.title}</h3></div><em>{human(i.severity)}</em></div><p>{i.why}</p><div className="radar-action"><CheckCircle2 size={16}/><span>{i.action}</span></div></article>)}{!r.items.length&&<EmptyState icon={ShieldCheck} title="Nenhum alerta calculado" text="O radar depende das informações registradas e não substitui inspeção profissional."/>}</div><div className="pass-principle warning"><div><AlertTriangle size={23}/><h3>Radar preventivo, não laudo.</h3></div><p>A aplicabilidade de normas, medidas de controle e liberações deve ser definida pelos profissionais responsáveis conforme a condição real do canteiro.</p></div></div>}

function AI({b}:{b:Bundle}){const[msgs,setMsgs]=useState<{role:'user'|'assistant';text:string}[]>([{role:'assistant',text:`Eu sou a Obra IA. Consulto gates, medições, passaporte técnico e riscos registrados em ${b.project.name}. Quando não houver evidência suficiente, vou dizer isso.`}]);const[text,setText]=useState('');const[busy,setBusy]=useState(false);async function ask(q=text){if(!q.trim()||busy)return;setMsgs(m=>[...m,{role:'user',text:q}]);setText('');setBusy(true);try{const r=await api(`/api/pass/projects/${b.project.id}/ai`,{method:'POST',body:JSON.stringify({question:q})});setMsgs(m=>[...m,{role:'assistant',text:r.answer}])}catch(e:any){setMsgs(m=>[...m,{role:'assistant',text:e.message}])}finally{setBusy(false)}}const sug=['Posso liberar o próximo pagamento?','O que falta para liberar a próxima etapa?','Há risco alto na próxima atividade?','Onde passa a tubulação registrada?','Quais garantias estão cadastradas?'];return <div className="pass-page ai-pass"><Head tag="OBRA IA" title="Uma IA que consulta o DNA da obra." text="Respostas baseadas nos registros do empreendimento, não em suposições."/><div className="ai-pass-shell"><aside><Sparkles size={22}/><h3>Contexto ativo</h3><p>{b.project.name}</p><ul><li>{b.gates.length} gates</li><li>{b.payments.length} medições</li><li>{b.passport.length} itens no passaporte</li><li>{b.riskRadar.items.length} alertas</li></ul></aside><section><div className="ai-messages">{msgs.map((m,i)=><div className={`ai-msg ${m.role}`} key={i}>{m.role==='assistant'&&<span><Bot size={17}/></span>}<p>{m.text}</p></div>)}{busy&&<div className="ai-msg assistant"><span><Bot size={17}/></span><p>Analisando registros…</p></div>}</div><div className="ai-suggestions">{sug.map(s=><button key={s} onClick={()=>ask(s)}>{s}</button>)}</div><div className="ai-input"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')ask()}} placeholder="Pergunte para o histórico técnico…"/><button onClick={()=>ask()} disabled={busy}><Send size={18}/></button></div></section></div></div>}

function Manage({b,reload}:{b:Bundle;reload:()=>void}){const[tab,setTab]=useState<'gate'|'evidence'|'payment'|'passport'>('gate');const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>,url:string,method='POST'){e.preventDefault();setMsg('');const body=Object.fromEntries(new FormData(e.currentTarget).entries());try{await api(url,{method,body:JSON.stringify(body)});setMsg('Salvo com sucesso.');(e.currentTarget as HTMLFormElement).reset();reload()}catch(err:any){setMsg(err.message)}}return <div className="pass-page"><Head tag="GESTÃO TÉCNICA" title="Alimente o que será verificável." text="O valor do produto nasce da rastreabilidade por etapa, não de números automáticos sem evidência."/><div className="mgmt-tabs">{[['gate','Gate'],['evidence','Evidência'],['payment','Medição'],['passport','Passaporte']].map(([k,l])=><button key={k} className={tab===k?'active':''} onClick={()=>setTab(k as any)}>{l}</button>)}</div><div className="mgmt-card">{tab==='gate'&&<form onSubmit={e=>{const fd=new FormData(e.currentTarget);const gid=fd.get('gateId');submit(e,`/api/pass/projects/${b.project.id}/gates/${gid}`,'PATCH')}}><FormTitle title="Atualizar gate" text="Libere uma etapa somente após a validação profissional dos requisitos aplicáveis."/><Fields><label>Gate<select name="gateId">{b.gates.map(g=><option key={g.id} value={g.id}>{g.phase_name}</option>)}</select></label><label>Status<select name="status"><option value="pending_review">Em validação</option><option value="blocked">Bloqueada</option><option value="released">Liberada</option></select></label><label>Score técnico<input name="technicalScore" type="number" min="0" max="100"/></label><label className="wide">Observações<textarea name="notes" rows={4}/></label></Fields><Save msg={msg}/></form>}{tab==='evidence'&&<form onSubmit={e=>submit(e,`/api/pass/projects/${b.project.id}/evidence`)}><FormTitle title="Registrar evidência" text="Foto, medição, documento, inspeção ou ensaio vinculado a um gate."/><Fields><label>Gate<select name="gateId">{b.gates.map(g=><option key={g.id} value={g.id}>{g.phase_name}</option>)}</select></label><label>Tipo<select name="evidenceType"><option value="photo">Foto</option><option value="measurement">Medição</option><option value="document">Documento</option><option value="inspection">Inspeção</option><option value="test">Ensaio</option></select></label><label className="wide">Título<input name="title" required/></label><label className="wide">Descrição<textarea name="description" rows={4}/></label><label className="wide">URL<input name="url" type="url"/></label></Fields><Save msg={msg}/></form>}{tab==='payment'&&<form onSubmit={e=>submit(e,`/api/pass/projects/${b.project.id}/payments`)}><FormTitle title="Criar liberação financeira" text="Registre a diferença entre o solicitado e o valor tecnicamente validado."/><Fields><label>Gate<select name="gateId">{b.gates.map(g=><option key={g.id} value={g.id}>{g.phase_name}</option>)}</select></label><label>Recomendação<select name="recommendation"><option value="release">Liberar</option><option value="partial">Liberar parcialmente</option><option value="hold">Não liberar</option></select></label><label>Solicitado<input name="claimedAmount" type="number" step="0.01"/></label><label>Validado<input name="validatedAmount" type="number" step="0.01"/></label><label className="wide">Descrição<input name="description" required/></label><label className="wide">Justificativa<textarea name="reason" rows={4}/></label></Fields><Save msg={msg}/></form>}{tab==='passport'&&<form onSubmit={e=>submit(e,`/api/pass/projects/${b.project.id}/passport`)}><FormTitle title="Adicionar item ao DNA do imóvel" text="Registre o que poderá ser útil quando estiver escondido ou anos depois da entrega."/><Fields><label>Sistema<select name="systemType"><option value="hidraulica">Hidráulica</option><option value="eletrica">Elétrica</option><option value="impermeabilizacao">Impermeabilização</option><option value="estrutura">Estrutura</option><option value="gas">Gás</option><option value="equipamento">Equipamento</option><option value="garantia">Garantia</option></select></label><label>Ambiente<input name="zone"/></label><label className="wide">Título<input name="title" required/></label><label>Altura (cm)<input name="heightCm" type="number" step="0.1"/></label><label>Profundidade (cm)<input name="depthCm" type="number" step="0.1"/></label><label>Garantia até<input name="warrantyUntil" type="date"/></label><label>Fornecedor<input name="supplier"/></label><label className="wide">Descrição<textarea name="description" rows={4}/></label><label className="wide">URL da foto<input name="photoUrl" type="url"/></label></Fields><Save msg={msg}/></form>}</div></div>}

function Fields({children}:{children:any}){return <div className="fields">{children}</div>}
function FormTitle({title,text}:{title:string;text:string}){return <div className="form-title"><h3>{title}</h3><p>{text}</p></div>}
function Save({msg}:{msg:string}){return <div className="save-row">{msg&&<span>{msg}</span>}<button><CheckCircle2 size={16}/> Salvar registro</button></div>}
function Metric({icon:I,label,value,text}:{icon:any;label:string;value:string;text:string}){return <article className="pass-metric"><I size={19}/><small>{label}</small><b>{value}</b><span>{text}</span></article>}
function Status({value}:{value:string}){return <span className={`pass-status ${value}`}>{human(value)}</span>}
function EmptyState({icon:I,title,text}:{icon:any;title:string;text:string}){return <div className="pass-empty"><I size={30}/><h3>{title}</h3><p>{text}</p></div>}
function gateLabel(s:string){return ({released:'Liberada',blocked:'Bloqueada',pending_review:'Em validação',draft:'Não iniciada'} as any)[s]||human(s)}

function NoProject({user,staff,reload,logout}:{user:User;staff:boolean;reload:()=>void;logout:()=>void}){const[open,setOpen]=useState(false);const[msg,setMsg]=useState('');async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();try{await api('/api/projects',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget).entries()))});reload()}catch(err:any){setMsg(err.message)}}return <div className="pass-empty-page"><div className="pass-empty-top"><Logo/><button onClick={logout}><LogOut size={16}/> Sair</button></div><div className="pass-empty-center"><ShieldCheck size={42}/><span className="kicker">PASSAPORTE DA OBRA</span><h1>{staff?'Cadastre a primeira obra real.':'Nenhuma obra vinculada.'}</h1><p>{staff?'O sistema não cria exemplos fictícios. Ao cadastrar uma obra, ele gera os gates-base para a equipe revisar.':'Peça à equipe Obra360 para vincular seu acesso.'}</p>{staff&&!open&&<button className="approve" onClick={()=>setOpen(true)}><Plus size={16}/> Criar obra</button>}{staff&&open&&<form className="new-project" onSubmit={create}><label>Nome<input name="name" required/></label><label>Município<input name="city"/></label><label>Orçamento<input name="budget" type="number"/></label><button className="approve">Criar e gerar gates</button>{msg&&<span>{msg}</span>}</form>}</div></div>}
